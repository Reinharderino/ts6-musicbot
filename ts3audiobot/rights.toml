# TS3AudioBot rights.toml — allow everything for all clients.
# The Python bot enforces its own permission logic; TS3AudioBot is the voice layer only.

# The root rule: any client can use any command.
"+"
